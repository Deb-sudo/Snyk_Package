{
  "name": "vulnerable-demo-app",
  "version": "1.0.0",
  "description": "A test app with outdated dependencies",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "4.16.0",               // known issues in older versions
    "lodash": "4.17.11",               // had prototype pollution
    "mongoose": "5.2.0",               // old version with known bugs
    "handlebars": "4.0.11",            // vulnerable to RCE in some versions
    "debug": "2.6.8"                   // had ReDos issue
  }
}
